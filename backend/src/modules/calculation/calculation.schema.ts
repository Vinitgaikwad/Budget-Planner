import zod from "zod"

/*

expense : 
    {
        acc_type: =>Time
        expense_type => Time
        budget_type => Time: Default Mounthly
    }

    current_accounts: account[].acc_type - expense[].acc_type

*/